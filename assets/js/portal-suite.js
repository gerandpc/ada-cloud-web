(function(){
  const ROLE_HOME={admin:'dashboard.html',directivo:'dashboard.html',secretaria:'secretaria.html',docente:'mi-espacio-docente.html',preceptor:'mi-espacio-preceptor.html',alumno:'mi-espacio-alumno.html',familia:'mi-espacio-familia.html'};
  function page(){return location.pathname.split('/').pop()||'dashboard.html'}
  function productForPage(){const p=page();if(p==='inteligencia-institucional.html')return'intelligence';if(p==='ia.html')return'ai';return'gestion'}
  function profile(){return window.ADA_CONTEXT?.perfil||window.ADA_CURRENT_PROFILE||null}
  let detectedRole='';
  function role(){return String(detectedRole||profile()?.rol||document.body.dataset.rol||'').toLowerCase()}
  function addSwitcher(){if(document.querySelector('.ada-product-switcher'))return;const r=role();if(!r)return;const main=document.querySelector('main .module-view, main.module-shell, .module-shell .module-view');if(!main)return;const wrap=document.createElement('nav');wrap.className='ada-product-switcher';wrap.setAttribute('aria-label','Productos ADA');const active=productForPage();const links=[{id:'gestion',label:'ADA Gestión',href:ROLE_HOME[r]||'dashboard.html',roles:['admin','directivo','secretaria','docente','preceptor','alumno','familia']},{id:'intelligence',label:'ADA Intelligence',href:'inteligencia-institucional.html',roles:['admin','directivo']},{id:'ai',label:'ADA AI',href:'ia.html',roles:['admin','directivo','secretaria','docente','preceptor','alumno','familia']}].filter(x=>x.roles.includes(r));wrap.innerHTML='<strong>Plataforma ADA</strong>'+links.map(x=>`<a href="${x.href}" class="${x.id===active?'active':''}" aria-current="${x.id===active?'page':'false'}">${x.label}</a>`).join('');main.insertBefore(wrap,main.firstChild)}
  function normalizeEmpty(){document.querySelectorAll('.role-list,.portal-list,.table-wrap').forEach(box=>{if(box.children.length===0&&!box.textContent.trim()){const d=document.createElement('div');d.className='ada-empty-state';d.textContent='No hay información disponible para mostrar.';box.appendChild(d)}})}
  function addStatus(){const view=document.querySelector('.module-view');if(!view||view.querySelector('.ada-portal-status')||page()==='inteligencia-institucional.html')return;const s=document.createElement('div');s.className='ada-portal-status';s.textContent='Información actualizada según tus permisos y asignaciones.';const hero=view.querySelector('.role-hero,.portal-hero');if(hero)hero.insertAdjacentElement('afterend',s)}
  function run(){addSwitcher();addStatus();setTimeout(normalizeEmpty,900)}
  document.addEventListener('DOMContentLoaded',()=>setTimeout(run,0));window.addEventListener('ada:role-applied',(event)=>{detectedRole=String(event.detail?.rol||'').toLowerCase();run()});window.addEventListener('ada:context-ready',run);
})();
